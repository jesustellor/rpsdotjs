/* display rock paper scissors and ask user to pick one
 * then display computer's pick and who won
 */

while(true) {

    // get user input
    let userInput = prompt("Pick rock, paper or scissors");
    userInput = userInput.toLowerCase();
}